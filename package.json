{
  "name": "pre-pr-ui-audit",
  "version": "1.0.0",
  "private": true,
  "description": "Diff-driven WCAG AA + reflow audit gate for changemaker-gcp",
  "scripts": {
    "test": "playwright test",
    "resolve": "node scripts/resolve-surfaces.mjs"
  },
  "devDependencies": {
    "@playwright/test": "1.55.0",
    "typescript": "^5.6.0"
  }
}
